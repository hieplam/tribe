{
  "name": "@tribe/campaign-runner",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Stateless deterministic card-loop capability for the tribe plugin (Claude Agent SDK executor per staged roadmap card).",
  "scripts": {
    "test": "bun test",
    "check": "bunx tsc --noEmit && bun test"
  },
  "dependencies": {
    "@anthropic-ai/claude-agent-sdk": "^0.3.278",
    "zod": "^4.4.3"
  },
  "devDependencies": {
    "@types/bun": "^1.3.14",
    "typescript": "^7.0.2"
  }
}

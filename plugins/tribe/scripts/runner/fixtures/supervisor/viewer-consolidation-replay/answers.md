## R09 — synthesized pre-existing owner ruling

ratified-as: pending

Synthesized placeholder body for a ruling recorded before this replay's three escalation
rounds begin — never the real ruling's own text. Mirrors the real `viewer-consolidation`
campaign's own exit code 5 shape (`rulings_unratified`): one ruling still unratified by the
time `runner_done` fires, requiring a `ratify` session before the campaign can close.

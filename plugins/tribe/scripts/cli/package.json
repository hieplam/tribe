{
  "name": "@tribe/cli",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "The `tribe` command: starts the read-only session viewer (more subcommands later).",
  "bin": {
    "tribe": "./bin/tribe"
  },
  "scripts": {
    "test": "bun test",
    "check": "bunx tsc --noEmit && bun test"
  },
  "devDependencies": {
    "@types/bun": "^1.3.14",
    "typescript": "^7.0.2"
  }
}

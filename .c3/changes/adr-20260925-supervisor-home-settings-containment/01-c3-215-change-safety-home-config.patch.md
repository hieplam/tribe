---
target: c3-215
scope: block
base: c3-215#n2084@v1:sha256:5218a11f1d13bee603f8de64e3efcd7010b7a67607b6cc598f60655a74e6fe53
---
| A ruling/ratify session writes outside the campaign home, or the repo is touched by a session that should never write to it | Editing core/supervisor/permit.ts (buildContainmentHook/decideContainmentHook/containPath/decideClosingGrantHook/buildHomeConfigWriteHook), core/supervisor/home-config.ts (isHomeConfigSurface/planHomeConfigRestore), session.ts's permissionMode/allowedTools/disallowedTools/additionalDirectories/settingSources construction for any of the three kinds, its scan-wall wiring (SCAN_GUARD_ENTRY, the shared decideScanGuardHook), or runOneShotSession's snapshot/restore of the campaign home's configuration surface (adapters/home-config.adapter.ts) | Measured (spec §19.4): with the containment hook removed, the identical permissionMode: 'default' envelope wrote to the repo, to /tmp, and through a symlink out of the home — the hook is the ONLY enforcement layer, additionalDirectories is a read convenience never a write boundary. All three kinds load settingSources ['user', 'project', 'local'] (card supervisor-session-settings, G3), so the 'user' tier registers the C3 plugin (Skill c3 loads) in every kind. ruling/ratify carry allowedTools [Read, Grep, Glob, Write, Edit, Skill] by owner ruling R2 (card supervisor-session-settings) — Skill only; the C3 CLI still cannot run there without Bash, a known, accepted limit — and disallowedTools [Bash, Task, Agent, WebFetch, WebSearch, Monitor, ScheduleWakeup]; the PreToolUse containment hook (decideContainmentHook) permits the read-only tools Read, Grep, Glob and a Skill load at any location plus a Write/Edit ONLY under the campaign home, denying every other tool fail-closed (R13.1, spec §5.1), and runs alongside the scan-wall hook (decideScanGuardHook, imported never copied — card D3), wired as defence in depth since this envelope already denies Bash (card D2). ruling gets repo READ via additionalDirectories [repoRoot]; ratify gets no repo access. The supervisor independently proves the target repo untouched before/after every ruling (git status --porcelain), failing the ruling on any change regardless of what the hook allowed. closing is the named exception: it carries its OWN explicit grant allowedTools [Read, Grep, Glob, Write, Edit, Bash, Skill] and disallowedTools [Task, Agent, WebFetch, WebSearch, Monitor, ScheduleWakeup] — the full Stage-D toolset and STILL NO containment hook, because it legitimately lands the governance PR (R11, owner ruling) — but it now carries three hooks of its own: the same scan-wall hook (defence in depth), decideClosingGrantHook, a pure allowlist predicate enforcing CLOSING_ALLOWED_TOOLS itself so a permissions.allow rule loaded from any of the three settings tiers can never widen the grant — MEASURED: with the tiers loaded and no grant hook, an un-granted tool (Workflow) ran (card supervisor-session-settings, ruling R1; accepted consequence: closing stops running TaskCreate/CronList/ListAgents, which ran un-granted before and which the closing stage does not use) — and buildHomeConfigWriteHook, refusing a Write/Edit to a configuration surface inside the home only (card supervisor-home-settings-containment). MEASURED 2026-09-25 (card supervisor-home-settings-containment, spec §4): the campaign home doubles as the NEXT session's settings root — a hooks block in <home>/.claude/settings.json or <home>/.claude/settings.local.json ran at SessionStart/UserPromptSubmit/every PreToolUse/Stop, and <home>/CLAUDE.md, CLAUDE.local.md, a nested or lower-case claude.md, .claude/skills and .mcp.json all loaded, in ruling, ratify AND closing sessions — so decideContainmentHook now denies a ruling/ratify Write/Edit to a configuration surface inside the home (isHomeConfigSurface, core/supervisor/home-config.ts: any path segment .claude, any last segment matching claude*.md, or .mcp.json, all case-insensitive) with its own reason, HOME_CONFIG_DENIED_REASON, never the out-of-home containment reason; and because a closing session's single Bash command planted all three files at once and the next session ran every hook (spec §4.4 pair e2, a write no Write/Edit PreToolUse hook can ever see), runOneShotSession also snapshots the home's configuration surface before every spawn — refusing to spawn at all when it cannot be read — and restores it to that snapshot after the session, whoever wrote the change, failing the session to outcome: 'error' when the home cannot be restored (rule-session-cwd-config-restored, fail-closed-edges.md) | cd plugins/tribe/scripts/runner && bun test core/supervisor/permit.test.ts core/supervisor/session.test.ts core/supervisor/home-config.test.ts adapters/home-config.adapter.test.ts && bunx tsc --noEmit; plugins/tribe/scripts/tests/test-supervisor-permission-real.sh (opt-in, real Haiku session, both directions: hook present denies the repo/tmp/symlink writes, hook removed lets them all land); core/supervisor/session.e2e.test.ts (opt-in RUN_SESSION_E2E=1, card supervisor-session-settings) — real Haiku sessions through the supervisor's own spawn path (runOneShotSession + the real SDK adapter, never a stub) proving Skill c3 returns C3 content in all three kinds, a real find / is refused by the scan wall in closing, and a host permissions.allow rule for Workflow still cannot run it in closing; core/supervisor/home-config.e2e.test.ts (opt-in RUN_SESSION_E2E=1, card supervisor-home-settings-containment) — two real sessions per test in the SAME campaign home proving a ruling/closing writer's planted hook and CLAUDE.md instruction never reach the next ruling/closing session, 0/3 before -> 3/3 after (docs/superpowers/evidence/2026-09-25-supervisor-home-settings-containment.md) |

---
target: c3-215
scope: block
base: c3-215#n2007@v1:sha256:41ca41b8c043dcbf95ff5f6b48354bcf657b179dd32f0ba60ca4becca5b53a43
---
| A ruling/ratify session writes outside the campaign home, or the repo is touched by a session that should never write to it | Editing core/supervisor/permit.ts (buildContainmentHook/decideContainmentHook/containPath), or session.ts's permissionMode/allowedTools/disallowedTools/additionalDirectories construction for ruling/ratify | Measured (spec §19.4): with the containment hook removed, the identical permissionMode: 'default' envelope wrote to the repo, to /tmp, and through a symlink out of the home — the hook is the ONLY enforcement layer, additionalDirectories is a read convenience never a write boundary. ruling/ratify carry allowedTools [Read, Grep, Glob, Write, Edit] and disallowedTools [Bash, Task, Agent, WebFetch, WebSearch, Monitor, ScheduleWakeup]; the PreToolUse containment hook (decideContainmentHook) permits the read-only tools Read, Grep, Glob at any location plus a Write/Edit ONLY under the campaign home, denying every other tool fail-closed (R13.1, spec §5.1). ruling gets repo READ via additionalDirectories [repoRoot]; ratify gets no repo access. The supervisor independently proves the target repo untouched before/after every ruling (git status --porcelain), failing the ruling on any change regardless of what the hook allowed. closing is the named exception: it carries its OWN explicit grant allowedTools [Read, Grep, Glob, Write, Edit, Bash, Skill] and disallowedTools [Task, Agent, WebFetch, WebSearch, Monitor, ScheduleWakeup] — the full Stage-D toolset and no containment hook — because it legitimately lands the governance PR (R11, owner ruling); settingSources ['project'] grants nothing on its own when the target repo has no committed .claude/settings.json, which is WHY closing needs the explicit grant | cd plugins/tribe/scripts/runner && bun test core/supervisor/permit.test.ts core/supervisor/session.test.ts && bunx tsc --noEmit; plugins/tribe/scripts/tests/test-supervisor-permission-real.sh (opt-in, real Haiku session, both directions: hook present denies the repo/tmp/symlink writes, hook removed lets them all land) |

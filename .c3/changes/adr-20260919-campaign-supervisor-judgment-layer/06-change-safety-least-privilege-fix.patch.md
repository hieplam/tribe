---
target: c3-215
scope: block
base: c3-215#n2007@v1:sha256:c06d8fe40d2519f903bd459d7b300bb8b6ad5a16344355d6eaaf76416504014d
---
| A ruling/ratify session writes outside the campaign home, or the repo is touched by a session that should never write to it | Editing core/supervisor/permit.ts (buildContainmentHook/decideContainmentHook/containPath), or session.ts's permissionMode/allowedTools/disallowedTools/additionalDirectories construction for ruling/ratify | Measured (spec §19.4): with the containment hook removed, the identical permissionMode: 'default' envelope wrote to the repo, to /tmp, and through a symlink out of the home — the hook is the ONLY enforcement layer, additionalDirectories is a read convenience never a write boundary. ruling gets repo READ only (additionalDirectories: [repoRoot]); ratify gets no repo access; both carry disallowedTools: [Bash, Task, Agent, WebFetch, WebSearch, Monitor, ScheduleWakeup]. The supervisor independently proves the target repo untouched before/after every ruling (git status --porcelain), failing the ruling on any change regardless of what the hook allowed. closing is the named exception (full toolset incl. Bash and repo write, no containment hook) because it legitimately lands the governance PR | cd plugins/tribe/scripts/runner && bun test core/supervisor/permit.test.ts core/supervisor/session.test.ts && bunx tsc --noEmit; plugins/tribe/scripts/tests/test-supervisor-permission-real.sh (opt-in, real Haiku session, both directions: hook present denies the repo/tmp/symlink writes, hook removed lets them all land) |

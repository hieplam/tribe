---
target: c3-215
scope: insert
base: c3-215#n1795@v1:sha256:28f22344eac8aeafc892845dc7461500e7c0576a71d68a4c929cc1b17ecad8af
---
| A campaign closes on a session's word when its cards did not actually verify-ship | Editing core/supervisor/verify.ts#verifyClosing or the loop's readShippedVerdicts edge (core/supervisor/loop.ts) | verifyClosing used to close on any non-empty final report, so a report whose body said BLOCKED still closed and nothing checked that verify-shipped actually ran (G3). It now takes shippedVerdicts (one per card the campaign report marks `shipped`) and closes ONLY when every one has a present, well-formed, matching, PASS verdict file — the file verify-shipped.sh --verdict-out wrote, not the session's prose. A missing/malformed/card-mismatch/FAIL verdict is a typed retryable `failed` attempt (verdict_missing/verdict_malformed/verdict_card_mismatch/verdict_fail), never a new ParkReason (the fence freezes that union); the existing bounded-retry-then-park(closing_failed) path handles it. verifyClosing is pure — the loop reads each file, this decides (pure-core.md) | cd plugins/tribe/scripts/runner && bun test core/supervisor/verify.test.ts && bunx tsc --noEmit — all five verdict rows plus the success path; and the end-to-end drive that a closing session writing no verdict file parks not exits zero: plugins/tribe/scripts/tests/test-supervisor-e2e.sh |

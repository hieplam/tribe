---
target: c3-215
scope: insert
base: c3-215#n1783@v1:sha256:a107f3067d9171725a2b151d6b6692b50b8d13bd8dcb1f06f30a876bf2fe7ebb
---
| scripts/ratchet-check.ts (context-budget ratchet gate) | IN | The enforcement point G2 lacked: `reviseCeiling` was a correct predicate with no caller, so a hand-edited unjustified ceiling raise passed every gate. `bun ratchet-check.ts --repo <dir> --base <ref> [--path <ratchet-path>]` (default docs/superpowers/evidence/2026-09-18-supervisor-ratchet.json) is a thin impure edge (pure-core.md): it reads two ratchet-file CONTENTS — the merge-base version via `git show <base>:<path>` and the working-tree version off disk — and hands both to the pure core/metrics/ratchet-gate.ts#checkRatchetRevision, which calls the existing reviseCeiling once per ceiling kind and refuses any ceiling raised without a matching top-level `raisedBy.<kind>` ruling id. An absent/unparseable base file reads as the initial measurement (every base ceiling is 0); an unparseable HEAD or missing "ceilings" object fails closed. The comparison is against the merge base, never HEAD~1, so a rebased raise is still caught and master's own past raises are never flagged. Exit 0 when clean, 1 printing each refusal. The git call is bounded by a timeout and host-config-isolated (fail-closed-edges.md obligations 2, 3) and a flag missing its value refuses rather than consuming the next token (obligation 1). Enforcement is wired through plugins/tribe/scripts/tests/test-supervisor-ratchet.sh, swept by pre-gate.sh's `test-*.sh` glob so no audit round can forget it — a committed unjustified raise turns that suite red | bun CLI edge + globbed shell test, repo-invoked (never installed) | plugins/tribe/scripts/runner/core/metrics/ratchet-gate.test.ts, plugins/tribe/scripts/tests/test-supervisor-ratchet.sh |

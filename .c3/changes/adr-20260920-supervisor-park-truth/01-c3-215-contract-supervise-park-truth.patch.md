---
target: c3-215
scope: block
base: c3-215#n1977@v1:sha256:a107f3067d9171725a2b151d6b6692b50b8d13bd8dcb1f06f30a876bf2fe7ebb
---
| scripts/runner/run.ts supervise (campaign supervisor) | IN | A subcommand of the existing runner CLI (no new resolver, no new installable): bun run.ts supervise --repo <path> --model <name> (--campaign <slug> or --home <path>, mutually exclusive) [--watchdog-model <name>] [--max-ruling-rounds N] [--max-ratify-rounds N] [--max-spawns N] [--max-watchdog-runs N] [--session-timeout-seconds N] [--session-max-turns N] [--session-retries N] [--poll-seconds N]; --repo/--model are required with no default, --campaign and --home are mutually exclusive and exactly one is required, and every unknown flag is rejected by name. Drives a zero-token L2 loop — observe disk facts, decide over a pure frozen table, spawn a bounded one-shot ruling/ratify/closing session only when a fact positively establishes judgment is required, verify its work against typed disk postconditions (never the session's own claim), persist, repeat — over the watchdog underneath it, adopting a live watchdog run rather than double-launching one. Those observed disk facts now include <home>/runs/<runId>/run.json for every run the home carries (read through one narrow fail-closed parser, ascending by runId, a missing or malformed record degrading to "no entry" rather than throwing into the tick loop): each record's liveness (endedAt null and its recorded pid still alive), its finalisation (endedAt plus exitCode), and its own exit reason. So the watchdog's published terminal is an INPUT the disk can overrule, never a fact: a still-alive newer run re-triggers the watchdog once — bounded by --max-watchdog-runs and a one-shot stale_terminal budget — instead of parking, and a run that finalised while nobody was watching continues from its OWN reason, translated out of core/report.ts's ExitReason vocabulary into the watchdog's terminal-reason vocabulary (done to runner_done) and failing closed on any value the map does not carry, never substituted raw. The same disk truth is what row P2 consults before refusing a resume: a park whose stated condition the disk has already falsified is superseded, not obeyed (pure predicates in core/supervisor/truth.ts; the supersede_park action in core/supervisor/decide.ts), while a park that still holds refuses exactly as before. A live supervisor on the same home is adopted, never double-launched; a second concurrent instance is refused with exit 21. Writes exclusively under <home>/supervisor/** (state.json, ledger.jsonl, events.jsonl including the park_superseded record, status.json including counters.staleTerminals, park/<cardId>.json, sessions/<sessionId>.log, final-report.md, .supervisor.lock) plus <home>/NEEDS_OWNER.md and its supersession rename to <home>/NEEDS_OWNER.md.superseded-<ts> — renamed, never deleted, so the owner's own document survives as the audit trail — and the <home>/escalations/<cardId>.md archive-on-ruling rename; it never writes answers.md, campaign-state.json, or anything under <home>/watchdog/ or <home>/runs/, which are read-only inputs to it. Exit codes: 0 done, 1 usage error, 20 needs_owner (NEEDS_OWNER.md written; deleting it is the owner's resume signal, and the supervisor may also supersede the park itself — park_superseded appended to events.jsonl, the rename above, counters.staleTerminals incremented, the campaign continuing instead of exiting 20 — but only for a runner-liveness park (stalled) that the disk contradicts AND for which a remedy is still available; every other park, and a stalled park whose re-trigger budget is spent, still refuses and still waits for the owner), 21 already_running | bun CLI subcommand, repo-invoked (never installed) | plugins/tribe/scripts/runner/core/supervisor/loop.test.ts, plugins/tribe/scripts/runner/core/supervisor/truth.test.ts, plugins/tribe/scripts/runner/core/supervisor/args.test.ts |

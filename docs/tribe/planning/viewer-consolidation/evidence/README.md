# Evidence index — viewer-consolidation (spec §16, plan Task 33)

One row per goal, the artifact that proves it, and the real measured number beside it. Spec §2's
goal map (quoted): *"G1, G2, G4 and G6 are user-visible claims, so each is proved in a real browser
against the real server, not at an API or unit boundary."* For G1, G2, G4 and G6 the artifact below
is the **DOM-level proof** named in spec §16 — a unit test is listed only as *supporting*, per that
same section, never as the proof itself.

All commands below run from `plugins/tribe/scripts/viewer` (`V/`). All pass counts and numbers are
freshly re-run against HEAD (`9f32872`) on 2026-09-17.

| Goal | Artifact | Measured |
| --- | --- | --- |
| **G1** | `e2e/dom-kinds.e2e.test.ts` — real headless Chromium against a fixture tree built from nothing (`~/.tribe` absent), driven by the runtime `RENDER_NODE_KINDS` witness (`core/model.ts`) | **31 pass / 0 fail**, one test per each of the **12** `RENDER_NODE_KINDS` (`prompt`, `assistant`, `thinking`, `tool`, `orphan_result`, `image`, `attachment`, `chip`, `divider`, `error`, `raw`, `unreadable`), plus one DOM assertion per distinguishable input shape (string vs array prompt, pending/ok/error tool, orphan result, empty vs non-empty thinking, base64 image, compaction divider, spill, unknown row) and the D10 project-window listing both ways |
| **G2** | `e2e/live-tail.e2e.test.ts` + `e2e/output/latency.json` (git-ignored, cited by path/number — see below) | Worst sample **243.5 ms** (243.45450000000164 ms) ≤ 1000 ms budget, across **44 samples**. §8.3 scroll readings (also in `latency.json`): `scroll-follow` 166.66 ms (scrollTop advanced while following), `scroll-away` 118.26 ms (scrollTop byte-identical before/after append while scrolled away from bottom, asserted `toBe` in the test), `resume` 194.81 ms (scrollTop returns to the bottom band after clicking the follow-pill). All well inside the 1 s budget |
| **G3** | `e2e/campaign-badge.e2e.test.ts` — real campaign runner, real Haiku 4.5 | Both stdout lines captured **verbatim** (see `evidence/campaign-badge-commands.md`): `campaign viewer: http://127.0.0.1:4399/?campaign=-private-var-folders-t2-s0b8z5m947l7kdcvwtrtrt480000gn-T-vc-badge-repo-MfvjBh/e2e-campaign-badge (read-only)` and `card C1: http://127.0.0.1:4399/s/440ded79-5552-412e-b3bb-44aab45769cb`. Two badges rendered and both filter legs proved by clicking the badge element (`evidence/campaign-badge-list.png`, `evidence/campaign-badge-filtered.png`) |
| **G4** | DOM/containment proofs: `core/paths.containment.test.ts` + `adapters/readonly.test.ts` + `serve.security.test.ts` + `structure.test.ts` (the zero-write wall), plus the fixture-tree before/after hash inside `e2e/dom-kinds.e2e.test.ts` | **135 pass / 0 fail** total (`paths.containment.test.ts` 5, `readonly.test.ts` 12, `serve.security.test.ts` 32, `structure.test.ts` 86); `dom-kinds.e2e.test.ts`'s fixture-tree hash is asserted `toBe` identical before/after the whole DOM run (no write actually happened) |
| **G5** | `deletion-guard.test.ts` (grep guard over the package) — spec §16's named artifact for G5 | **48 pass / 0 fail**. Confirms the status page, `/live`, scan adapter, session-tail reader, and `--tribe-root` are gone, and no `runs/*/logs/` read remains in the package (rule 6 also feeds G4) |
| **G6** | `e2e/served-build.e2e.test.ts` — real rebuild into the real `dist/`, hashed, served, refetched, hash-compared | **3 pass / 0 fail**. `GET /` and `GET /index.html` bodies sha256-match `dist/index.html` exactly (`hashAfter === hashBefore` for the fixture tree too — no write). Plus `test-install-viewer-build.sh`: **3 passed, 0 failed** |

## §14 performance budgets — all 7 green

Source: `e2e/output/perf.json` (git-ignored; regenerated by `TRIBE_VIEWER_E2E=1 bun test
e2e/perf.test.ts`, `measuredAt: "2026-09-17T14:54:07.272Z"`, this file's current content at HEAD
`9f32872` — the 0e4dc10..9f32872 merge of origin/master touched no file under `viewer/` or
`runner/`, so this run remains valid evidence for the current code). `e2e/output/latency.json`
(`measuredAt: "2026-09-17T15:09:48.486Z"`) covers the append→browser row.

| Budget | Target | Measured | Pass |
| --- | --- | --- | --- |
| `GET /api/projects`, cold | ≤ 2.0 s | 275.52 ms (22 projects in the D10 window) | yes |
| `GET /api/projects`, warm | ≤ 150 ms | 48.26 ms | yes |
| open a 13,178,184-byte session, first `rows` frame | ≤ 1.0 s | 111.28 ms (384 nodes, byte range 2,657,179–13,178,184) | yes |
| back-fill 500 more nodes | ≤ 400 ms | 56.27 ms | yes |
| append → browser (G2) | ≤ 1 s | 243.5 ms worst of 44 samples (`latency.json`) | yes |
| 8 concurrent streams, RSS | ≤ 800 MB (D34/R18) | 307,625,984 B = **293.4 MB** | yes |
| 8 concurrent streams, RSS after 10 min of appends | ≤ 800 MB (D34/R18) | 103,333,888 B = **98.5 MB** (120 rows appended over 600,000 ms) | yes |
| single SSE frame (initial window, largest real transcript) | ≤ 1 MiB | 376,271 B ≈ **376 KB** (2 frames in the initial burst) | yes |

An earlier 2026-09-14 run (recorded in the campaign's own worker-report log, before the real
corpus grew from 181 to 224 sessions) measured the same 7 budgets at 137 ms / 47 ms / 87 ms / 54 ms
for the first four rows respectively; both runs pass every budget by a wide margin, and the
difference is real corpus growth on this machine between the two measurement dates, not a
regression — spec §14's own budget row is stated "over 181 sessions" (head 64 KiB + tail 256 KiB
**per session**), so `GET /api/projects, cold` is, by design, proportional to session count.

## Corpus-count reproduction (title window, spec §5.3 / R2)

```sh
cd plugins/tribe/scripts/viewer && bun tools/title-window.ts --head 65536 --tail 262144
```

Result: `223/224` (99.6%) ≥ the 99% acceptance threshold — see spec.md's Task-33 reconciliation
note (§5.3) for why the window constant itself is unchanged.

## Gate sweep

Verbatim output recorded in the Task-33 hunter report:
`~/.tribe/-Users-hip-repo-tribe/campaigns/viewer-consolidation/reports/viewer-consolidation.md`
(`tsc --noEmit` x2, `bun test` x2 — 863/3/0 viewer, 684/0/0 runner — the two install-hook/build
shell suites, `bunx @c3x/cli@11.6.3 check`, the fence-parity check, the glossary-citation check —
all green). `pre-gate.sh` and `gap-gate.ts` are deferred to the Warchief's own final Method-step-7
run, per this task's brief, and are not part of this index's claims.
